% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log.R
\name{handler_frames_on_stack}
\alias{handler_frames_on_stack}
\title{Is a calling-handler-establishing frame on the call stack?}
\usage{
handler_frames_on_stack()
}
\value{
\code{TRUE} if registration would be refused.
}
\description{
\code{globalCallingHandlers()} refuses to run while calling handlers are on the
stack, and every catching wrapper (\code{tryCatch}, \code{try},
\code{withCallingHandlers}, \code{suppressWarnings}, \code{suppressMessages}) establishes
one, so the refusal cannot itself be caught. This inspects the call stack
for those wrappers instead, so registration can be skipped gracefully
(e.g. under testthat) rather than erroring.
}
\keyword{internal}
