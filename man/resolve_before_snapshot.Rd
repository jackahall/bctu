% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{resolve_before_snapshot}
\alias{resolve_before_snapshot}
\title{Resolve the \code{before} argument of a report into a snapshot, or NULL}
\usage{
resolve_before_snapshot(before, after, verbose = 1L)
}
\arguments{
\item{before}{A \code{bctu_snapshot}, a character selector, or \code{NULL}.}

\item{after}{The current snapshot the report is about.}

\item{verbose}{Verbosity.}
}
\value{
A \code{bctu_snapshot}, or \code{NULL} for no comparison.
}
\description{
A \code{bctu_snapshot} passes through; \code{NULL} means no comparison. A character
selector is resolved from the store the \code{after} snapshot lives in (falling
back to the project store). The default selector \code{"penultimate"} resolves
to the snapshot immediately BEFORE \code{after} in that store, so a rerun on an
older snapshot compares against its own predecessor, never against newer
data. Every resolution failure (no store, no earlier snapshot, or an
earlier directory that cannot be loaded, e.g. a pre-rebuild snapshot with
no manifest) falls back to \code{NULL} with a message, never an error: an
uncompared report is always preferable to no report.
}
\keyword{internal}
