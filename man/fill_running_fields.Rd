% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_fields.R
\name{fill_running_fields}
\alias{fill_running_fields}
\title{Fill the fields of a header or footer part}
\usage{
fill_running_fields(xml, document)
}
\arguments{
\item{xml}{The header or footer part text.}

\item{document}{The document.xml text the \code{STYLEREF} fields refer to.}
}
\value{
The part text with the field results written.
}
\description{
A template's running header names the report through \code{STYLEREF} fields,
which Word only recomputes on an update, so a document opened without one
shows the template's own cached words. This evaluates each field the way
Word would and writes the result into the part: \code{STYLEREF} takes the
first paragraph of the named style, \code{IF} compares its two quoted
operands with \code{=} or \verb{<>}, and \code{PAGE} and \code{NUMPAGES} are given a result
region for Word to fill at layout. Nested fields are evaluated inside the
instruction of the field that holds them. A field of any other kind keeps
its cached result.
}
\keyword{internal}
