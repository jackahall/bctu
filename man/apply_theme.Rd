% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_postprocess.R
\name{apply_theme}
\alias{apply_theme}
\title{Apply a resolved theme to an unpacked report}
\usage{
apply_theme(work, values, template)
}
\arguments{
\item{work}{The unpacked docx folder.}

\item{values}{A resolved theme, from \code{\link[=resolve_theme]{resolve_theme()}}.}

\item{template}{The template specification, from \code{\link[=report_template]{report_template()}}:
its \code{fonts} and \code{font_size} are what the theme's replace.}
}
\value{
\code{work}, invisibly.
}
\description{
Each colour element is written to its theme slot in \code{theme1.xml} and to
the literal fallbacks beside every reference to that slot in the
document, styles, headers and footers (Word reads the slot, other
renderers the literal, so both agree and Word's Design menu still
works). Font elements replace the template's typefaces by role, and
\code{font.size} scales every size in the template.
}
\keyword{internal}
