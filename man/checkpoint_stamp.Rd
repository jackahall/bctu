% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log.R
\name{checkpoint_stamp}
\alias{checkpoint_stamp}
\title{The provenance stamp used in run logs and snapshot manifests}
\usage{
checkpoint_stamp(time = utc_now())
}
\arguments{
\item{time}{The stamp's UTC time; default now. Passing it lets a caller use
one clock read for the stamp and for anything derived from it.}
}
\value{
A list: \code{created_utc}, \code{r_version}, \code{bctu_version}, \code{user}, \code{host}.
}
\description{
The pure computation behind \code{\link[=checkpoint]{checkpoint()}}: the stamp alone, with no run-log
interaction, used internally wherever a stamp must never have side effects
(snapshot manifests, the run-log header itself).
}
\keyword{internal}
