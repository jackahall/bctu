% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recruitment.R
\name{count_by_month}
\alias{count_by_month}
\title{Count dates by calendar month, filling months with no events}
\usage{
count_by_month(dates, from = min(dates), to = max(dates))
}
\arguments{
\item{dates}{A Date vector (or anything \code{\link[=as.Date]{as.Date()}} accepts).}

\item{from}{First month to report. Default the earliest date.}

\item{to}{Last month to report. Default the latest date. Set it to the
data-freeze month to run the series to the freeze.}
}
\value{
A data frame with one row per month: \code{month} (Date, the first of
the month), \code{n}, \code{cum_n} and \code{cum_pct} (100 * \code{cum_n} / total counted).
}
\description{
Floors each date to the first of its month and counts them, with a row
for every month from \code{from} to \code{to} inclusive. Months where nothing
happened get \code{n = 0} rather than being left out, so a recruitment plot
or table shows the real gaps. Missing dates are dropped. Dates outside
\code{from} to \code{to} are not counted.
}
\examples{
count_by_month(as.Date(c("2026-01-15", "2026-03-02", "2026-03-20")))
}
