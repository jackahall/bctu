% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx_theme.R
\name{report_theme}
\alias{report_theme}
\title{Theme a BCTU trial report}
\usage{
report_theme(
  colour.accent = NULL,
  rule.colour = NULL,
  table.border.colour = NULL,
  table.header.fill = NULL,
  text.secondary.colour = NULL,
  link.colour = NULL,
  font.body = NULL,
  font.heading = NULL,
  font.title = NULL,
  font.code = NULL,
  font.size = NULL
)
}
\arguments{
\item{colour.accent}{The accent colour every other colour inherits from
(UoB gold, \code{"C59A00"}).}

\item{rule.colour}{The title-page and running-header rules and the
report-type line. Inherits \code{colour.accent}.}

\item{table.border.colour}{The top and bottom rules of every table.
Inherits \code{rule.colour}.}

\item{table.header.fill}{The shading of every table's header row.
Inherits a light tint of \code{colour.accent}.}

\item{text.secondary.colour}{Captions, the report subtype and the lower
headings (dark grey).}

\item{link.colour}{Hyperlinks (UoB blue).}

\item{font.body}{The body typeface (Arial).}

\item{font.heading}{The heading typeface. Inherits \code{font.body}.}

\item{font.title}{The title-page acronym typeface (Times New Roman).}

\item{font.code}{The code typeface (Consolas).}

\item{font.size}{The body size in points (11); every size in the
template scales with it.}
}
\value{
A \code{report_theme} object: a named list of the elements given.
}
\description{
Builds a theme for \code{\link[=trial_report]{trial_report()}} in the style of \code{\link[ggplot2:theme]{ggplot2::theme()}}:
every argument is one element of the Word template, elements inherit
from one another, and anything not given keeps the template default.
The same theme can be written in the YAML header under \verb{theme:}, with
the element names as keys.
}
\examples{
report_theme(colour.accent = "#0057BF")
report_theme(rule.colour = "0057BF", table.header.fill = "F5EDD4", font.size = 10)
}
