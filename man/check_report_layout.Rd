% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_layout.R
\name{check_report_layout}
\alias{check_report_layout}
\title{Check the page layout of a rendered report}
\usage{
check_report_layout(path, sparse = 6L)
}
\arguments{
\item{path}{Path to the docx.}

\item{sparse}{Pages with fewer body lines than this are reported.}
}
\value{
A data frame with one row per finding: \code{page}, \code{orientation},
\code{lines}, \code{issue} and \code{first} (the first body line). Zero rows means no
finding. \code{NULL}, with a message, when LibreOffice or pdftotext is not
available.
}
\description{
Converts the docx to PDF with LibreOffice (\code{soffice} on the PATH, or the
path in \code{options(bctu.soffice = )}) and reads it page by page with
\code{pdftotext}, flagging a caption left as the last line of a page (its table
or figure fell to the next page), a blank page, and a page with fewer
than \code{sparse} lines of body text. LibreOffice is a proxy for Word: it
ignores keep-with-next before a table and substitutes fonts, so a flagged
caption whose docx carries the keep flags may be its artefact.
}
\examples{
\dontrun{
check_report_layout("report.docx")
}
}
